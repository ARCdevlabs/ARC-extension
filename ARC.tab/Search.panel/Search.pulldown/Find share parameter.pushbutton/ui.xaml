<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SP Searcher" Height="400" Width="500" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nhập tên Parameter (Tiếng Nhật/Anh):" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="search_tb" Grid.Row="0" Height="30" 
                 VerticalContentAlignment="Center" Padding="5"
                 TextChanged="search_box_changed" Margin="0,20,0,10"/>

        <DataGrid x:Name="result_dg" Grid.Row="1" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" 
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Parameter Name" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Parameter Group" Binding="{Binding Group}" Width="*" FontWeight="Bold" Foreground="Blue"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="status_lb" Grid.Row="2" Text="Ready" Foreground="Gray" Margin="0,5,0,0" FontStyle="Italic"/>
    </Grid>
</Window>